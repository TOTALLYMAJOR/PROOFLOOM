# Codex Adapter

Use the install script to copy the four skills into the Codex skills directory:

```bash
./scripts/install-codex-skills
```

Then use the adapter `AGENTS.md` as a starting point in a consuming repository when that repository does not already have equivalent design instructions.

Do not let this adapter outrank an existing repository authority.
