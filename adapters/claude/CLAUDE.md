# Design Intelligence Adapter

- Existing repository authorities outrank Design Intelligence defaults.
- Run repo-fit before major interface or workflow changes.
- Use `design-language` for design language decisions.
- Use `ux-architect` for task organization and information hierarchy.
- Use `reference-intelligence` for reference analysis, not imitation.
- Use `visual-review` for rendered validation.
- Prefer knowledge alignment over runtime migration.
