export default {
  theme: {
    extend: {
      colors: {
        brand: "hsl(var(--brand))"
      },
      borderRadius: {
        lg: "var(--radius)"
      },
      fontFamily: {
        sans: ["Geist", "sans-serif"]
      }
    }
  }
};
