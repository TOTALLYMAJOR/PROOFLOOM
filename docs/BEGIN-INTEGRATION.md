# Begin integration: work-line pointer

Authority: instructions
Status: PREPARED_AWAITING_BEGIN
Prepared: 2026-09-18.

The current selected continuation is the native Proofloom / AgentFlow / Traffic-Control-Pilot integration. The full work directive and updated native task context are on branch `codex/proofloom-trust-repair-20260917` in this repository, at `docs/BEGIN-INTEGRATION.md` and `.dev/tasks/active/TASK-PROOFLOOM-AGENTFLOW-V2-ENFORCEMENT.json`.

Read the current work-line directive before acting on an explicit operator `begin`. Reference for the prepared task context: commit `19df0e82682e9a3725630130ef874b58517ecb4d`. Resolve current branch heads, instructions, source hashes, native task dependencies and local dirty state; a reference commit is not an execution approval. Main has not acquired the trust branch's implementation by the addition of this pointer.

This pointer is navigation, not a second backlog or copied work authority. Do not automatically merge, reset, force-push, switch a dirty checkout or discard unpublished work. Use an appropriate isolated checkout after native scope/authority resolution. Preserve AgentFlow's execution ownership and every existing human adoption, approval and acceptance gate.

The prepared sequence is branch/authority reconciliation, native v2 contract parity, AgentFlow admission, TCP-002/TCP-003 supervision and an audited local return loop. DecisionIntelligence is advisory and is not a prerequisite for the first ordinary successful run. Do not install exported V7-EPI task packs as canonical or reopen the selected architecture.

Preparation starts no implementation. `begin` is a natural-language operator instruction, not a new CLI command, automatic job or authenticated receipt. The first action remains read-only native preflight; real scope, authority, runtime-access and evidence blockers must be reported rather than bypassed.
