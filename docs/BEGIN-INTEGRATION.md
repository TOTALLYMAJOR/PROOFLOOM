# Begin: finish the existing governed integration

Authority: instructions
Status: PREPARED_AWAITING_BEGIN
Scope: work selection and launch procedure, not a runtime approval receipt.
Prepared: 2026-09-18.

The current conversation requested updates to the relevant work authorities so a later explicit `begin` is enough to resume the integration direction already present in the repositories. This document records that request; it does not authenticate a person, fabricate a signature, adopt a new product architecture, or claim implementation has started.

## Objective and ownership

Finish the existing Proofloom -> AgentFlow -> Traffic Control -> Proofloom local execution/evidence loop. Preserve design intelligence as Proofloom's purpose. AgentFlow owns planning, execution, validation, integration, runtime state and multi-repository coordination. Traffic Control Pilot evaluates continuation and proposes interventions. DecisionIntelligence is advisory structural analysis, not another scheduler or executor.

Use native tasks, contracts, receipts, storage and tests. Do not install the conversation-exported V7-EPI task packs or their receipt schemas as competing canonical systems. They are review material only. Defer a new cognitive runtime, adaptive routing, expanded strategy library and additional dashboards until the native integration works.

## What `begin` means

A later explicit operator message `begin` (case-insensitive, not a quoted example or text inside a file) selects this work and starts the procedure below. The present preparation request does not start implementation. Do not ask the operator to redesign this plan, paste another long prompt, or reconfirm this same objective.

Start with read-only preflight, then perform only the first eligible bounded task through the existing authorized execution path. Preserve progress in the existing task/evidence machinery and continue to the next eligible task while scope and native authority remain valid. `begin` is an operator instruction, not a cryptographic credential and not a substitute for distinct native backlog-adoption, execution-approval, direction-selection or acceptance records. Resolve those records through the real authority mechanism at the exact revision; never manufacture human identity, signatures, approval booleans or passing receipts. An unresolved authorization, missing credential, substantive scope change or unavailable execution capability is a real stop, not permission to bypass the gate.

## Checkout selection

| Repository | Work line to inspect | Reference observed during preparation |
| --- | --- | --- |
| TOTALLYMAJOR/PROOFLOOM | codex/proofloom-trust-repair-20260917 | 3f4479381eabbacb24fa584a93a3820ff959057c |
| TOTALLYMAJOR/AgentFlow | main | feb204155abf86adf22f9f3b501b2c639c7fec79 |
| TOTALLYMAJOR/Traffic-Control-Pilot | main | 16e8f56441bcfc1b75778d550aed4a76e62bfdd2 |
| TOTALLYMAJOR/DecisionIntelligence | main; advisory only for this slice | c4150ecce4b2d4e2364132cf15be56fa1ec99625 |

These are discovery references, not approvals for future commits. Read current branch heads and local dirty state before work. Preserve unpublished changes and isolated branches. Do not reset, force-push, merge the trust branch, merge AgentFlow's onboarding PR, switch a dirty checkout, or delete work to make this table appear current. Resolve exact approved bases after this instruction update. If starting on Proofloom main, inspect the named trust branch before proposing replacement implementation.

## Start procedure

1. **Reconcile the actual working state.** Read each repository's instructions, current implementation and executable contracts. Compare the trust branch with main. Run the existing Proofloom governance audit/verification and task-health checks against the intended checkout; a saved HUMAN-READOUT is not a fresh result. Record existing / partial / missing / blocked for the selected task dependency closure. Resolve broken source references from real repository evidence; do not invent missing product intent or recreate absent documents from guesses.
2. **Reuse the native prerequisite work.** Inspect the existing .dev task packets for HANDOFF-V2-SNAPSHOT, AGENTFLOW-RECEIPT-INTEGRITY and AGENTFLOW-V2-ENFORCEMENT, together with every declared prerequisite. Keep their existing IDs, dependencies and human acceptance rules. Revalidate code already present instead of rebuilding it because a task is still STAGED. Do not mark a prerequisite complete from source presence, documentation or a previous summary.
3. **Close protocol parity before live supervision.** Proofloom's selected trust branch declares design-intelligence/governed-task-handoff@2.0.0; Traffic Control main observed during preparation accepts 1.0.0 only. Implement and test a compatible, versioned native binding without discarding snapshot fields, defaulting missing authority or making v1 an executable fallback. Bind identical canonical payloads/digests across languages, including optional-field normalization. Derive the exact target write sets from the live files before native task adoption.
4. **Complete native AgentFlow admission and returned evidence.** Execute the AgentFlow side of TASK-PROOFLOOM-AGENTFLOW-V2-ENFORCEMENT under AgentFlow-owned scope. Verify repository identity, exact clean base, critical sources and authority currency before immutable-plan binding and again at required execution transitions. Preserve task IDs, dependencies, owns, acceptance, validation, produces/consumes and claim boundary. Map native task manifests into the existing build-receipt contract; do not rename a task manifest and call that interoperability.
5. **Connect the Governor, using the existing operationalization work.** TCP-002 supplies observations from native AgentFlow records; TCP-003 persists decisions and applies interventions through the existing coordinator/retry/integration paths. Protocol parity and v2 admission are cross-repository prerequisites, even where an older planning projection does not yet list them. Preserve the native dependency closure; add only the necessary target-repository binding through normal adoption. No second scheduler, retry engine, graph, execution database or universal event authority.
6. **Prove the local return loop.** Reuse TCP-006's receipt-audit work and the existing Proofloom traffic_control_audit/agentflow_contracts functions. Run a bounded temporary-fixture acceptance case through the real local services, including refusal paths. A synthetic policy demo alone does not satisfy this gate. Bind all repository revisions, handoff digest, actual changed paths, validation records, decisions and integration result.
7. **Stop at the stated milestone.** Report local acceptance evidence and remaining gaps. The existing TASK-PROOFLOOM-TRUST-PILOT-ACCEPTANCE still targets the QuoteFlow consuming-repository pilot; a temporary fixture does not complete it. Do not silently switch its target or write to QuotePilot/QuoteFlow without its own inspected authority, bounded task scope and approval. DecisionIntelligence participates only if a real structural reconsideration is raised; it is not a dependency of an ordinary successful first run.

## Existing authorities and task homes

- Proofloom: AGENTS.md, docs/TRUST_MODEL.md, docs/AGENTFLOW-INTEGRATION.md, docs/architecture/ADR-0004-authority-snapshot-and-drift-invalidation.md and .dev/tasks/active/.
- Selected Proofloom IDs: TASK-PROOFLOOM-HANDOFF-V2-SNAPSHOT, TASK-PROOFLOOM-AGENTFLOW-RECEIPT-INTEGRITY, TASK-PROOFLOOM-AGENTFLOW-V2-ENFORCEMENT; retain all prerequisites in their native JSON. TASK-PROOFLOOM-TRUST-PILOT-ACCEPTANCE remains the later consuming-repository acceptance authority.
- Traffic-Control-Pilot: README.md, docs/protocol.md and BACKLOG.md, especially TCP-002, TCP-003 and TCP-006. That BACKLOG is explicitly a planning projection; it does not authorize writes in another repository.
- AgentFlow: .github/copilot-instructions.md, reviewed .agentflow.yaml, its native planning/build/initiative services and committed backlog mechanism. Historical MRI-001 through MRI-008 are not automatically selected for rerun. Materialize only the selected, dependency-complete target task projection through the existing mechanism; retain source IDs and digests and do not create a competing backlog.
- DecisionIntelligence: AGENTS.md and the existing decision-contract/compiler authority. Draft alternatives are never execution approvals.

## Required proof for the first local milestone

- A current authorized v2 handoff enters the real AgentFlow plan/execution path; PROPOSED, stale, dirty, altered, wrong-repository and unsupported-version inputs do not.
- Dependency and artifact gates remain native. Work cannot advance from an unrelated receipt or an unintegrated dependency.
- Actual Git changes determine ownership; executor PASS fields do not establish proof. Existing protected tests, baselines and validation configuration cannot be silently weakened to pass.
- Native validation runs at the recorded code revision. Missing, substituted, failed or skipped required validation prevents acceptance. Record environment, command and inspectable evidence.
- Governor decisions come from native observations and preserve the original approved handoff. Pause/block/retry/repair remain bounded; replan/reconsider remain proposal-only. No live state mutation during replay.
- The integrated result returns the exact native build receipt, survives Proofloom audit, and fails audit after handoff, task-set, evidence or Governor-record tampering.
- Keep implementation, local validation, integration, deployment, customer acceptance and outcomes separate. Human acceptance is recorded only by the native permitted human mechanism.

## Scope and stop rules

Preparation changes instructions/task context only; it does not dispatch a worker. On begin, use isolated work and serialize shared-file integration. No deployment, provider configuration, production/consumer writes, credential rotation, force pushes, remote deletion, unapproved test-baseline changes or automatic main-branch merges. No fabricated runtime facts or network-isolation claims. A new security boundary or missing verifier must be separately bounded and approved, never silently supplied by the candidate it would admit.

Do not treat the exported handoff-v2 package name as the same version namespace as the native governed-task-handoff@2.0.0 protocol. Reconcile requirements into existing code rather than replacing native authority with the export. If live scope or authority cannot satisfy a required gate, preserve evidence and report one concrete blocker with its owning repository and recovery action.

Completion of this preparation means the next action and source authorities are recorded. It does not mean governance has converged, native approval is present, the integration is implemented, tests have passed, or the QuoteFlow pilot is accepted.
