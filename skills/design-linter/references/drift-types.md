# Drift Types

- Token drift: arbitrary colors, spacing, radius, or width values
- Component drift: competing primitives for the same responsibility
- Layout drift: overflow, unsafe widths, off-screen placement
- Accessibility drift: controls or images missing required semantics

Intentional product-specific exceptions are allowed, but they should be explicit.
